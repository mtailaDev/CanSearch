<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="8dp">

    <TextView
            android:id="@+id/eligibility_card_title"
            style="@style/SectionHeaderAppearance"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:text="@string/card_title_eligibility"/>

    <View
            style="@style/SectionHeaderAccentView"
            app:layout_constraintTop_toTopOf="@+id/eligibility_card_title"
            app:layout_constraintBottom_toBottomOf="@+id/eligibility_card_title"
            app:layout_constraintEnd_toEndOf="parent"/>

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/eligibility_recycler_view"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/eligibility_card_title"/>

</androidx.constraintlayout.widget.ConstraintLayout>

